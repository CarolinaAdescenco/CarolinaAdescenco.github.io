import { keyframes } from "styled-components"
import { fadeInDown, fadeInUp, fadeInLeft, fadeInRight } from "react-animations"

export const fadeDown = keyframes`${fadeInDown}`
export const fadeUp = keyframes`${fadeInUp}`
export const fadeLeft = keyframes`${fadeInLeft}`
export const fadeRight = keyframes`${fadeInRight}`
